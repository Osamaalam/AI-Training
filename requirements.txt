# Core Deep Learning & LLM/VLM Training Dependencies
torch>=2.2.0
torchvision
transformers>=4.45.0
peft>=0.12.0
trl>=0.10.0
datasets>=3.0.0
accelerate>=0.34.0
bitsandbytes>=0.43.0

# Unsloth Fast Finetuning Dependencies
# Note: unsloth, xformers, and unsloth_zoo are best installed matching 
# the specific CUDA and PyTorch versions of your target environment.
# e.g., pip install --no-deps unsloth unsloth_zoo
# pip install xformers

# Data Processing & Utilities
pandas>=2.0.0
numpy>=1.24.0
pillow>=10.0.0
pyyaml>=6.0
openpyxl>=3.1.0
tensorboard>=2.15.0

# Serialization & Tokenization
protobuf>=4.25.0
sentencepiece>=0.1.99
huggingface_hub>=0.25.0
hf_transfer>=0.1.8
